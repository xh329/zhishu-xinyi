# PRD.md — 今日热搜聚合产品（HotNow）

> 项目类型：课程实战 · Day 4 产品需求文档（PRD）
> 研究文档依据：`research.md`（同目录）
> 版本：v0.1（MVP 阶段） ｜ 最后更新：Day 4

---

## 0. 先讲清楚：PRD 是什么
**PRD（Product Requirements Document，产品需求文档）不是代码，而是"产品总纲"。**
它不关心用什么语言、什么框架（那是 Day 5 技术选型的事），只回答四件事：
1. **给谁用** —— 目标用户是谁；
2. **解决什么问题** —— 用户痛点是什么；
3. **MVP 有哪些功能** —— 第一版先做什么；
4. **哪些以后再做** —— 边界在哪，不做什么。

本 PRD 全程不写代码，只定义"做什么"和"做到什么程度算合格"。

---

## 1. 产品概述
- **产品名称**：今日热搜（HotNow）
- **一句话定位**：把各大平台今天的"热搜/热榜"聚合到一个页面，30 秒看懂今天大家在搜什么、为什么火，并能收藏感兴趣的话题。
- **项目归属**：本课程「第 1 周」实战项目。

### 1.1 目标用户
| 维度 | 描述 |
|------|------|
| 身份 | 18–35 岁碎片化时间多的手机网民（学生、上班族） |
| 场景 | 通勤、午休等碎片时间，想快速了解热点 |
| 诉求 | 不想来回切 App；想知道"为什么上热搜"；看到喜欢的想稍后看 |
| 非目标 | 专业舆情分析师、媒体从业者（超出 MVP） |

### 1.2 要解决的核心问题
1. **平台分散**：微博/百度/抖音/知乎各自有榜，用户切换成本高。
2. **只有标题缺解读**：看到热搜词却不懂"为什么火"。
3. **看不到留存**：感兴趣的话题，下次想找却找不到。

---

## 2. MVP 功能（必须做）
> 每个功能都写清：**用户动作** + **可观察结果**。MVP 共 3 个功能，均可在一天内拆成小任务。

### F1 聚合热搜榜首页
- **用户动作**：用户打开 App/网页，进入首页。
- **可观察结果**：首页按热度/时间聚合展示 ≥3 个平台（微博、百度、抖音）的今日热搜条目；每条显示「标题、来源平台、热度值、更新时间、排名」，并支持按平台筛选。

### F2 热搜详情页
- **用户动作**：用户在首页点击某条热搜条目。
- **可观察结果**：进入详情页，展示该话题的「一句话简介、2–3 条相关新闻摘要、近 24 小时热度趋势（简易曲线/数字）」，让用户 30 秒看懂"为什么火"。

### F3 收藏 / 订阅（写入功能）
- **用户动作**：用户在首页或详情页点击「收藏」某条热搜，或在设置中「订阅」某平台。
- **可观察结果**：收藏后该条目标记为已收藏状态（图标变化），并出现在「我的收藏」页；订阅的平台在首页置顶展示；数据写入本地/服务端，刷新或重新打开后依然保留。

---

## 3. 页面 / 视图规划（≥3 个）
| 页面/视图 | 作用 | 关键元素 |
|-----------|------|---------|
| **P1 首页（聚合热搜榜）** | 一屏看全部平台热点 | 平台筛选条、热搜列表（标题/平台/热度/时间）、收藏按钮 |
| **P2 热搜详情页** | 看单条话题的解读 | 简介、相关摘要、热度趋势、收藏按钮、返回 |
| **P3 我的 / 收藏页** | 看已收藏话题与订阅平台 | 收藏列表、订阅平台管理、清空 |
| **P4 设置页（轻量）** | 平台订阅开关、刷新 | 平台订阅开关、手动刷新按钮 |

### 3.1 数据字段
- **热搜条目（hot_item）**
  `id`(唯一) · `title`(标题) · `platform`(来源平台) · `heat`(热度值) · `rank`(排名) · `updated_at`(更新时间) · `category`(分类标签) · `summary`(详情摘要，详情页用)
- **收藏记录（favorite）**
  `user_id`(用户标识) · `item_id`(条目id) · `created_at`(收藏时间)
- **平台（platform）**
  `name`(平台名) · `enabled`(是否订阅) · `status`(接口状态：正常/异常)

---

## 4. 后续 / 暂不做（边界，明确理由）
> 本节是 MVP 与未来的**清晰分界**，每条都给理由，杜绝"以后再说但没边界"的空泛表述。

| 功能 | 状态 | 理由 / 边界 |
|------|------|------------|
| 个性化推荐算法 | **砍掉** | MVP 阶段无用户行为数据，做了也是瞎推；且算法复杂度高，违背"一天可拆小任务"原则。上线后凭真实数据再评估。 |
| 跨天历史归档检索 | **暂缓（Day 5+）** | 需要额外存储与检索能力，MVP 只保留当日数据即可验证需求，历史检索待数据量起来后再做。 |
| 评论 / 社区互动 | **暂缓** | 涉及内容审核与运营成本，超出 MVP 范围；先做信息聚合，互动留到增长期。 |
| 实时推送通知 | **暂缓** | 依赖后端长连接/推送能力，MVP 用"手动刷新"满足，推送待后端能力具备后评估。 |
| 海外 / 多语言市场 | **不做** | 与核心"中文互联网热点"场景无关，当前阶段明确排除。 |

---

## 5. 验收标准（可逐条检查）
> 用于 Day 4「AI 自检」与人工验收，每条 Yes/No 可判定。

- [ ] **AC1** PRD 明确写出目标用户（身份 + 场景 + 诉求）。
- [ ] **AC2** PRD 明确写出要解决的 3 个核心问题。
- [ ] **AC3** MVP 至少包含 3 个功能（F1/F2/F3），每个都有「用户动作 + 可观察结果」。
- [ ] **AC4** 至少规划 3 个页面/视图（P1/P2/P3，P4 为可选轻量页）。
- [ ] **AC5** 数据字段列表清晰（hot_item / favorite / platform）。
- [ ] **AC6** 有「后续/暂不做」分界，且每条带状态与理由，无空泛表述。
- [ ] **AC7** 随机抽取任一功能，能回答「谁 / 在什么页面 / 做什么 / 看到什么」。
- [ ] **AC8** 全文无"以后再说但没有边界"的空泛表述。
- [ ] **AC9** PRD 定位为产品总纲，不含任何代码实现。

---

## 6. 每日一问：你在 PRD 里砍掉了什么？为什么？
- **砍掉的功能**：个性化推荐算法。
- **为什么砍**：
  1. MVP 阶段没有任何用户行为数据，推荐无从训练，做了也是随机推，反而损害体验；
  2. 推荐系统开发与调参成本高，无法在"一天内拆成小任务"，违背 MVP 轻量验证的初衷；
  3. 产品当前核心假设是"用户想要一站式看热点"，应先验证这个假设，再谈个性化。
- **暂缓但未砍**：历史归档、社区互动、实时推送——这些不是错误方向，只是当前阶段数据/能力不具备，明确标注了未来触发的边界条件。

---

## 7. 余力加练：用户故事（F2 热搜详情页）
> 作为「用户故事」格式示例，非必须交付。

**标题**：作为关注热点的普通用户，我希望点击一条热搜就能看到它为什么火，以便我在 30 秒内判断要不要深入了解。

**验收条件（Given/When/Then）**：
- **Given** 用户在首页看到一条来自微博的热搜"XX 事件"；
- **When** 用户点击该条目；
- **Then** 系统打开详情页，展示：一句话简介、≥2 条相关新闻摘要、近 24 小时热度趋势；
- **And** 页面提供「收藏」按钮，点击后状态持久化到「我的收藏」。

---

## 附：MVP 功能可拆解性自检（实战步骤 2）
| 功能 | 是否能一天内拆成小任务 | 拆解示例 |
|------|----------------------|---------|
| F1 首页聚合 | 是 | 拉取接口→列表渲染→平台筛选→热度排序 |
| F2 详情页 | 是 | 路由→简介展示→摘要列表→趋势图 |
| F3 收藏写入 | 是 | 收藏按钮→写库→我的页读取→刷新保留 |
